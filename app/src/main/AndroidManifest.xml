<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.projeto.projetopi">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icone"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".HistoricoPresenca"
            android:label="@string/title_activity_historico_presenca"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity
            android:name=".AlunoHome"
            android:label="@string/title_activity_aluno_home"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ListaChamada"
            android:label="@string/title_activity_lista_chamada"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".AlunosPresentes"
            android:label="@string/title_activity_alunos_presentes"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ProfessorHome"
            android:label="@string/title_activity_professor_home"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".HomeActivity"
            android:label="@string/title_activity_home"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ui.login.LoginActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>